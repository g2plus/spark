<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.arhi.mapper.LogMapper">
    <insert id="logInfo">
        insert into log (out_name,in_name,money,`date`,status)
        values (#{outName},#{inName},#{money},#{date},#{status})
    </insert>
</mapper>